#!/bin/sh
set -e

INIT_TAG="/root/.init_tag_do_not_delete"

if [ ! -f "$INIT_TAG" ]; then
    echo "[INIT] First run, initializing root directory..."
    mkdir -p /root
    tar -zxf /build/res/root.tar.gz -C /
    rm -rf /root/.ssh
    mkdir -p /work/config/ssh
    chmod 700 /work/config/ssh
    [ -f /work/config/ssh/authorized_keys ] && chmod 600 /work/config/ssh/authorized_keys
    ln -s /work/config/ssh /root/.ssh
    echo "[INIT] Done"
else
    echo "[INIT] Already initialized, skipping..."
fi
