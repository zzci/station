#!/bin/sh

if [ ! -f "/root/.init_tag_do_not_delete" ]; then
    echo "Init default root file"
    mkdir -p /root
    tar -zxvf /build/res/root.tar.gz -C /
    mkdir -p /work/config/ssh-local
    ln -s /work/config/ssh-local /root/.ssh
fi
